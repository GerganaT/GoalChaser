<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_menu_root"
    tools:showIn="navigation_view">

    <group
        android:id="@+id/drawer_menu_group"
        android:checkableBehavior="single">
        <item
            android:id="@+id/instructions_menu"
            android:icon="@drawable/ic_instructions"
            android:title="@string/instructions_drawer_menu_item" />
        <item
            android:id="@+id/statistics_menu"
            android:icon="@drawable/ic_statistics"
            android:title="@string/statistics_drawer_menu_item" />
        <item
            android:id="@+id/completed_goals_menu"
            android:icon="@drawable/ic_completed_goals"
            android:title="@string/completed_goals_drawer_menu_item" />
        <item
            android:id="@+id/about_menu"
            android:icon="@drawable/ic_about"
            android:title="@string/about_drawer_menu_item" />
    </group>
</menu>