package com.example.android.goalchaser.background

class ImageDataWorker {
    //TODO Load image once  per day ; don't allow new image to be loaded unless it's a new day
    // Delete the previously loaded image if there's more than one loaded.
}